from django.apps import AppConfig


# Mason has added a comment in the apps file 
# (just making sure github is working)
class BillingConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'billing'
