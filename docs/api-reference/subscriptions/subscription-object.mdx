---
title: "Subscription Object"
---

<ParamField body="customer" type="object" required>
  <Expandable title="">
    <ResponseField name="customer_name" type="string or null" >
    </ResponseField>

    <ResponseField name="customer_id" type="string" required />
    <ResponseField name="email" type="string <email>" required />
    <ResponseField name="payment_provider" type="(PaymentProviderEnum (string or null)) or (BlankEnum (any or null)) or (NullEnum (any or null))" required />

    <ParamField body="properties" type="object or null">
        <Expandable title="">
            <ResponseField name="property name" type="string">
            additional property
            </ResponseField>
        </Expandable>

    </ParamField>

    <ParamField body="integration" type="object or null">
        <Expandable title="">
            <ResponseField name="property name" type="string">
            additional property
            </ResponseField>
        </Expandable>

    </ParamField>

    </Expandable>

</ParamField>

<ParamField body="start_date" type="string <date-time>" required>
  the date that the subscription started or was renewed.
</ParamField>

<ParamField body="end_date" type="string <date-time>" />

<ParamField
  body="scheduled_end_date"
  type="string or null <date-time>"
  required
/>

<ParamField body="status" type="string (Status2b4Enum)">
  Enum: `"active"` `"ended"` `"not_started"`
</ParamField>

<ParamField body="auto_renew" type="boolean" />

<ParamField body="is_new" type="boolean" />

<ParamField body="subscription_id" type="string" />

Subscriptions link a customer to a plan, for a given time period. Therefore, tehre may not be more than one subscription for a given customer and plan at a given time.
